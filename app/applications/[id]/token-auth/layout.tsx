export const metadata = {
  title: 'Token Authentication - Wafie Console',
  description: 'Configure API token-based authentication for application access'
};

export default function TokenAuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
