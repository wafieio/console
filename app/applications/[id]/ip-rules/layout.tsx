export const metadata = {
  title: 'IP Rules - Wafie Console',
  description: 'Configure IP-based access control rules and restrictions'
};

export default function IPRulesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
