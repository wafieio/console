export const metadata = {
  title: 'AntiBot Protection - Wafie Console',
  description: 'Configure bot detection and mitigation settings'
};

export default function AntiBotLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}